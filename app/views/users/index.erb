<!DOCTYPE html>
<html>
    <head>
        <title>All Users!!</title>
    </head>
    <body>
        <h1>All Registered Users</h1>
        <% @users.each do |u|%>
            <ul>
            <li><a href="/users/<%= u.id%>/show"><%= u.name%><br><%= u.email%></a></li>        
            </ul>
            <% end %>            
    </body>
</html>