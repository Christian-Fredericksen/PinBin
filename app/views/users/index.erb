<!DOCTYPE html>
<html>
    <head>
        <title>All Users!!</title>
    </head>
    <body>
        <h1>All Registered Users</h1>
        <% @users.each do |u|%>
            <ul>
            <li>
            <a href="/other_collections/<%= u.id%>"><%= u.name%><br><%= u.email%></a>        
            </ul>
            <% end %>            
    </body>
</html>